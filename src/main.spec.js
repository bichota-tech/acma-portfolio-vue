import { describe, it, expect } from 'vitest'

describe('Project Setup', () => {
  it('should have Vue 3 installed', () => {
    // Este es un test básico para verificar que Vitest funciona
    expect(true).toBe(true)
  })

  it('should have pinia for state management', () => {
    expect(true).toBe(true)
  })

  it('should have vue-router for routing', () => {
    expect(true).toBe(true)
  })
})
